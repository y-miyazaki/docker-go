#!/bin/bash
set -eu

if [ "$DOCKER_TAG" == "latest" ]; then
    docker build --build-arg -t ${IMAGE_NAME} -f ${DOCKERFILE_PATH} .
else
    docker build --build-arg GOLANG_VERSION=${DOCKER_TAG} -t ${IMAGE_NAME} -f ${DOCKERFILE_PATH} .
fi
